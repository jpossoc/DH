
create table if not exists odontologos_tabla (
    id int auto_increment primary key,
    numero_matricula int,
    nombre varchar(255),
    apellido varchar (255)
    );


    -- agregar acá sentencias Insert para precargar datos.